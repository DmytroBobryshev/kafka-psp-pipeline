<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example.psp</groupId>
    <artifactId>kafka-psp-pipeline</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>psp-connector</artifactId>
  <packaging>jar</packaging>

  <name>psp-connector</name>
  <description>
    psp-connector (M4): first consumer. Consumes payments.payment-requested.v1, simulates a
    payment provider (configurable latency/decline/timeout), publishes
    payments.payment-status-changed.v1 keyed by merchantId. Hexagonal layout enforced by an
    ArchUnit test; MapStruct at every boundary, same conventions as payment-api (M3).
  </description>

  <dependencies>
    <!-- Internal libs -->
    <dependency>
      <groupId>com.example.psp</groupId>
      <artifactId>common-events</artifactId>
    </dependency>
    <!-- M18 phase 2: Kafka-aware HealthIndicators (kafkaListeners / kafkaStreams) so the
         Kubernetes readiness probe reflects the listener containers, not just the servlet.
         Inert unless psp.health.kafka.enabled=true, which only the k8s ConfigMap sets. -->
    <dependency>
      <groupId>com.example.psp</groupId>
      <artifactId>common-health</artifactId>
    </dependency>

    <!-- M16/ADR-0009: Eureka client - compose profile only, same convention as every other
         service (see payment-api/pom.xml's identical comment). Registers this service so
         api-gateway can resolve lb://psp-connector, even though ADR-0004 means it exposes no
         business REST endpoint - only actuator (below). -->
    <dependency>
      <groupId>org.springframework.cloud</groupId>
      <artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
    </dependency>

    <!-- Web + actuator: no business REST endpoints in M4 (ADR-0004 - inter-service traffic is
         Kafka-only), but actuator health/info needs a web server to expose HTTP, and M8's DLQ
         replay endpoint will live here later. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- M15: Micrometer Tracing bridged to OpenTelemetry, exporting to Tempo over OTLP - see
         payment-api/pom.xml's comment for why no <version> is needed. -->
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-tracing-bridge-otel</artifactId>
    </dependency>
    <dependency>
      <groupId>io.opentelemetry</groupId>
      <artifactId>opentelemetry-exporter-otlp</artifactId>
    </dependency>

    <!-- Real Postgres persistence (ADR-0005: psp-connector owns its own database, psp_connector) -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <!-- Flyway >= 10 split DB-specific support out of flyway-core -->
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>

    <!-- Kafka consumer (this module's headline: M4's first consumer) AND producer (publishes
         payments.payment-status-changed.v1) -->
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka</artifactId>
    </dependency>

    <!-- M9 Phase 1: payments.payment-requested.v1 is now Avro + Schema Registry - the INBOUND
         side only. Outbound payments.payment-status-changed.v1 stays JSON (spring-kafka's
         JsonSerializer, config.KafkaProducerConfig, unchanged). -->
    <dependency>
      <groupId>org.apache.avro</groupId>
      <artifactId>avro</artifactId>
    </dependency>
    <dependency>
      <groupId>io.confluent</groupId>
      <artifactId>kafka-avro-serializer</artifactId>
    </dependency>

    <!-- Lombok + MapStruct (annotation processor order fixed in the parent) -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct</artifactId>
    </dependency>

    <!-- Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.kafka</groupId>
      <artifactId>spring-kafka-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- In-memory DB for the plain context-load test - the real Postgres migration is exercised
         for real against the live compose stack, not here (same pattern as payment-api). -->
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>psp-connector</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>repackage</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
