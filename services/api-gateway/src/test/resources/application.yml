spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes: []

eureka:
  client:
    enabled: false

api-gateway:
  startup:
    require-known-profile: false
